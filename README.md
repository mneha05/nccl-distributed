NCCL Distributed Example

This repo contains notes and run scripts for launching distributed training with NCCL and PyTorch's distributed launcher. It is a scaffold — requires multiple nodes/GPUs to fully test.
